---
description: "查看 DCP 统计：真实发送 token、裁剪节省、各策略命中、缓存命中率（会话+累计）"
allowed-tools: "mcp__dcp__dcp_stats"
---

请调用 dcp_stats 工具（MCP 工具，名称类似 mcp__dcp__dcp_stats）获取 DCP 统计数据，然后把返回的文本原样展示给我（用代码块包裹）。不要总结、不要改写——我要看原始数字。
