---
description: "重新应用被解压的 DCP 压缩块（无参=列出可重应用项）"
argument-hint: "[blockId]"
allowed-tools: "mcp__dcp__dcp_recompress"
---

请调用 dcp_recompress 工具（MCP 工具，名称类似 mcp__dcp__dcp_recompress）$ARGUMENTS（无参不带参数=列出；有参数则 blockId=<n>）。展示返回文本。
